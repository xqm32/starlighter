[project]
name = "starlighter"
version = "0.1.0"
description = "Star Lighter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28.1"]

[dependency-groups]
dev = ["astrbot>=4.26.3"]
